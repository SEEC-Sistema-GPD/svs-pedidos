version: '3.8'

services:
  web:
    build: .
    ports:
      - "56:80"
    # Se a aplicação necessitar persistência de dados (uploads, logs, etc.), configure os volumes:
    # volumes:
    #   - ./svs-pedidos/uploads:/var/www/html/uploads
